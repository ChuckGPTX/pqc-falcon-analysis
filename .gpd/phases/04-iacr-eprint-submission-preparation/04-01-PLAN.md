---
phase: 04-iacr-eprint-submission-preparation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - paper/main.tex
  - paper/main.pdf
  - paper/BUILD-VALIDATION.md
  - paper/BIBLIOGRAPHY-AUDIT.json
  - paper/reproducibility-manifest.json
interactive: true

conventions:
  units: "UTF-8 text, security levels in base-2 bits, and repo-relative file paths"
  metric: "not_applicable"
  coordinates: "manuscript source tree, local TeX toolchain, and ePrint-facing PDF constraints"

approximations:
  - name: "local LaTeX toolchain as build proxy"
    parameter: "pdflatex or latexmk availability in the execution environment"
    validity: "Phase 4 build validation before submission packaging"
    breaks_when: "no TeX engine is available locally or the manuscript depends on undeclared packages/files"
    check: "paper/BUILD-VALIDATION.md records the exact toolchain, command sequence, and any hard blocker"
  - name: "inline bibliography may already be the flattened final state"
    parameter: "paper/main.tex currently contains an inline bibliography rather than an external .bib file"
    validity: "acceptable only if the final source tree builds cleanly with no hidden bibliography dependency"
    breaks_when: "citation cleanup introduces an external dependency without capturing the corresponding generated artifact"
    check: "paper/BIBLIOGRAPHY-AUDIT.json states the final bibliography strategy explicitly and the clean build reproduces it"

contract:
  schema_version: 1
  scope:
    question: "What build-validation and bibliography-finalization work is required to turn the reviewed manuscript into a clean, reproducible PDF source tree for ePrint submission?"
  claims:
    - id: claim-buildable-source
      statement: "The reviewed manuscript source tree builds cleanly into a submission-ready PDF with no missing local inputs, missing packages, or unresolved cross-references."
      deliverables: [deliv-build-note, deliv-built-pdf, deliv-final-manuscript]
      acceptance_tests: [test-latex-build]
      references: [ref-manuscript, ref-gap-table, ref-artifact-manifest, ref-repro-manifest, ref-eprint-operations]
    - id: claim-bibliography-ready
      statement: "The manuscript's bibliography is final, complete, and explicitly documented as a submission-safe source strategy rather than a hidden or ad hoc dependency."
      deliverables: [deliv-build-note, deliv-bibliography-audit, deliv-final-manuscript]
      acceptance_tests: [test-bibliography-ready]
      references: [ref-manuscript, ref-bibliography-audit, ref-eprint-submit]
  deliverables:
    - id: deliv-build-note
      kind: note
      path: paper/BUILD-VALIDATION.md
      description: "Build-validation note recording the LaTeX dependency graph, toolchain used, command sequence, and any blocker encountered while generating the final PDF."
      must_contain:
        - "resolved input graph"
        - "exact build command sequence"
        - "final blocker status"
    - id: deliv-built-pdf
      kind: data
      path: paper/main.pdf
      description: "Submission-ready PDF generated from the current manuscript source."
      must_contain:
        - "current manuscript title"
        - "all section and table content"
    - id: deliv-final-manuscript
      kind: report
      path: paper/main.tex
      description: "Finalized LaTeX source for the ePrint-bound manuscript after build and citation cleanup."
      must_contain:
        - "faithful statement of falcon_security_reduction"
        - "full 12-gap taxonomy"
        - "supporting surrogate-model caveats"
    - id: deliv-bibliography-audit
      kind: data
      path: paper/BIBLIOGRAPHY-AUDIT.json
      description: "Refreshed bibliography audit that records the final citation coverage and bibliography strategy used for the submission source tree."
      must_contain:
        - "final bibliography strategy"
        - "citation coverage status"
        - "remaining issues field set to empty or explicit blockers"
  references:
    - id: ref-manuscript
      kind: prior_artifact
      locator: paper/main.tex
      role: must_consider
      why_it_matters: "Primary LaTeX source that must build cleanly and preserve the reviewed theorem and framing claims."
      applies_to: [claim-buildable-source, claim-bibliography-ready]
      carry_forward_to: [execution, verification, writing]
      must_surface: true
      required_actions: [read, use, compare]
    - id: ref-gap-table
      kind: prior_artifact
      locator: paper/tables/gap-summary.tex
      role: must_consider
      why_it_matters: "Local input file that must resolve during LaTeX compilation and remain synchronized with the manuscript."
      applies_to: [claim-buildable-source]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-artifact-manifest
      kind: prior_artifact
      locator: paper/ARTIFACT-MANIFEST.json
      role: must_consider
      why_it_matters: "Current inventory of the manuscript package and supporting artifact paths."
      applies_to: [claim-buildable-source]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read, compare]
    - id: ref-bibliography-audit
      kind: prior_artifact
      locator: paper/BIBLIOGRAPHY-AUDIT.json
      role: must_consider
      why_it_matters: "Current statement of bibliography coverage and the place where the final bibliography strategy must be recorded."
      applies_to: [claim-bibliography-ready]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read, use, compare]
    - id: ref-repro-manifest
      kind: prior_artifact
      locator: paper/reproducibility-manifest.json
      role: must_consider
      why_it_matters: "Records the current known build blocker (`pdflatex` unavailable) and must be updated when the toolchain or build status changes."
      applies_to: [claim-buildable-source]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read, use, compare]
    - id: ref-eprint-submit
      kind: other
      locator: https://eprint.iacr.org/submit
      role: definition
      why_it_matters: "Defines the current submission-form expectations, including PDF upload, abstract constraints, keywords, and metadata alignment."
      applies_to: [claim-bibliography-ready]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read, compare, use]
    - id: ref-eprint-operations
      kind: other
      locator: https://eprint.iacr.org/operations.html
      role: definition
      why_it_matters: "States the archive's acceptance conditions, including first-page author/contact requirements and readable PDF expectations."
      applies_to: [claim-buildable-source]
      carry_forward_to: [execution, verification]
      must_surface: true
      required_actions: [read, compare, use]
  acceptance_tests:
    - id: test-latex-build
      subject: claim-buildable-source
      kind: reproducibility
      procedure: "Audit the local input graph, then run pdflatex or latexmk until the manuscript build stabilizes; if no TeX engine is available, stop at a checkpoint and record the exact blocker in paper/BUILD-VALIDATION.md."
      pass_condition: "paper/main.pdf exists, all local inputs resolve, and the final build has no undefined-reference, undefined-citation, or missing-file errors."
      evidence_required: [deliv-build-note, deliv-built-pdf, deliv-final-manuscript, ref-gap-table, ref-repro-manifest, ref-eprint-operations]
      automation: hybrid
    - id: test-bibliography-ready
      subject: claim-bibliography-ready
      kind: consistency
      procedure: "Compare the final manuscript source and PDF against the refreshed bibliography audit to confirm that every citation is resolved and the source tree's bibliography strategy is explicit."
      pass_condition: "paper/BIBLIOGRAPHY-AUDIT.json states a final strategy (inline-final or generated bibliography artifact), and the clean build proves there is no hidden bibliography dependency."
      evidence_required: [deliv-build-note, deliv-bibliography-audit, deliv-final-manuscript, deliv-built-pdf, ref-bibliography-audit, ref-eprint-submit]
      automation: hybrid
  forbidden_proxies:
    - id: fp-warning-ridden-pdf
      subject: claim-buildable-source
      proxy: "Producing a PDF while leaving unresolved citations, missing-file fallbacks, or first-pass build warnings untreated."
      reason: "A visually plausible PDF is not a submission-ready source tree if the build is still fragile or non-reproducible."
    - id: fp-handwave-bibliography
      subject: claim-bibliography-ready
      proxy: "Declaring the bibliography 'flattened' without recording whether the final state is inline, generated .bbl, or another explicit source artifact."
      reason: "Submission preparation requires an auditable bibliography strategy, not an implicit assumption."
  links:
    - id: link-build-note
      source: claim-buildable-source
      target: deliv-build-note
      relation: supports
      verified_by: [test-latex-build]
    - id: link-built-pdf
      source: claim-buildable-source
      target: deliv-built-pdf
      relation: supports
      verified_by: [test-latex-build]
    - id: link-bibliography-audit
      source: claim-bibliography-ready
      target: deliv-bibliography-audit
      relation: supports
      verified_by: [test-bibliography-ready]
  uncertainty_markers:
    weakest_anchors:
      - "The current environment still may not expose a working TeX toolchain even though a clean PDF build is required."
      - "The inline bibliography may already be the correct final state, but that must be demonstrated explicitly rather than assumed."
    disconfirming_observations:
      - "If pdflatex or latexmk remains unavailable after toolchain probing, build validation is blocked and the phase cannot advance automatically."
      - "If a supposedly clean build still reports unresolved citations, undefined references, or missing local inputs, the manuscript is not submission-ready."
---

<objective>
Make the reviewed manuscript buildable and bibliography-finalized for ePrint submission.

Purpose: Clear the concrete Phase 4 blocker that still separates the reviewed draft from a real upload package.
Output: A cleanly built `paper/main.pdf`, a refreshed bibliography audit, and a build-validation note that records the final source strategy.
</objective>

<execution_context>
@/Users/chuckcrawley/.codex/get-physics-done/workflows/execute-plan.md
@/Users/chuckcrawley/.codex/get-physics-done/templates/summary.md
</execution_context>

<context>
@.gpd/PROJECT.md
@.gpd/ROADMAP.md
@.gpd/STATE.md
@.gpd/phases/04-iacr-eprint-submission-preparation/04-RESEARCH.md
@paper/main.tex
@paper/tables/gap-summary.tex
@paper/ARTIFACT-MANIFEST.json
@paper/BIBLIOGRAPHY-AUDIT.json
@paper/reproducibility-manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the LaTeX source tree and lock the bibliography strategy</name>
  <files>paper/main.tex, paper/tables/gap-summary.tex, paper/ARTIFACT-MANIFEST.json, paper/BUILD-VALIDATION.md, paper/reproducibility-manifest.json</files>
  <action>Inspect the manuscript input graph, document every local dependency needed to build the PDF, confirm whether the current inline bibliography is already the intended final state or whether a generated bibliography artifact will be required, and write the result to paper/BUILD-VALIDATION.md before attempting the final build.</action>
  <verify>Confirm that every local input resolves, that the build note lists the exact toolchain and command sequence to be used, and that paper/reproducibility-manifest.json reflects the current toolchain assumptions.</verify>
  <done>paper/BUILD-VALIDATION.md records a closed input graph, an explicit bibliography strategy, and the exact current toolchain status.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 2: Resolve any hard LaTeX-environment blocker before the final build</name>
  <files>paper/BUILD-VALIDATION.md, paper/reproducibility-manifest.json</files>
  <action>If no working TeX engine is available locally, stop and present the exact missing tool or failing package/class to the user. Request a LaTeX-capable environment or permission to continue there. If a working TeX engine is already available, skip the checkpoint and continue automatically.</action>
  <verify>The blocker note names the exact failing command, missing binary, or missing package/class instead of reporting a vague build failure.</verify>
  <done>A working TeX path is available for the build, or the user has explicitly redirected execution to an environment where the build can run.</done>
</task>

<task type="auto">
  <name>Task 3: Build the PDF, clear citation issues, and refresh the submission audits</name>
  <files>paper/main.tex, paper/main.pdf, paper/BUILD-VALIDATION.md, paper/BIBLIOGRAPHY-AUDIT.json, paper/reproducibility-manifest.json</files>
  <action>Run the chosen LaTeX build sequence until the PDF stabilizes, fix any source or citation issues revealed by the build, update paper/BIBLIOGRAPHY-AUDIT.json with the final bibliography strategy, and refresh paper/reproducibility-manifest.json so the Phase 4 build state is accurate.</action>
  <verify>Require a build with no unresolved citations, no undefined references, no missing local files, and a PDF that satisfies the ePrint readability constraints for A4 or US letter.</verify>
  <done>`paper/main.pdf` exists, the build note records a clean command sequence, and the refreshed bibliography/reproducibility audits agree with the final source tree.</done>
</task>

</tasks>

<verification>
- Compare the final build note against the generated PDF and refreshed manifests so the manuscript, bibliography strategy, and environment description all tell the same story.
- Reject any output that preserves placeholder author/contact metadata only because the Phase 3 draft happened to build.
- Preserve theorem fidelity and the no-attack boundary while making build-oriented edits.
</verification>

<success_criteria>
- `paper/main.pdf` builds cleanly from the recorded source tree.
- `paper/BUILD-VALIDATION.md` records the exact toolchain, command sequence, and final blocker status.
- `paper/BIBLIOGRAPHY-AUDIT.json` states the final bibliography strategy explicitly and reports no unresolved citation-coverage issue.
- `paper/reproducibility-manifest.json` no longer contradicts the actual local build state.
</success_criteria>

<output>
After completion, create `.gpd/phases/04-iacr-eprint-submission-preparation/04-01-SUMMARY.md`.
</output>
